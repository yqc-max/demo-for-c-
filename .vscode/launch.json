{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Mac C Debug",
            "type": "cppvsdbg", // 或者尝试用 "cppdbg"
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "environment": [],
            "externalConsole": false, // 在 Mac 上 false 默认就会走集成终端
            "preLaunchTask": "C/C++: clang 生成活动文件"
        },
        {
            "type": "lldb", // 只要装了 CodeLLDB 扩展，这里改用 lldb
            "request": "launch",
            "name": "Debug",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${fileDirname}",
            "preLaunchTask": "C/C++: clang 生成活动文件"
        }
    ]
}